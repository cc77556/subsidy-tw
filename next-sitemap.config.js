/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://subsidy.tw',
  generateRobotsTxt: false, // We use app/robots.ts instead
  generateIndexSitemap: false,
  outDir: './public',
};
